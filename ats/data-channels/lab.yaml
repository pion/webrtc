version: "3.9"
services:
  runner:
    build: 
      context: .
      dockerfile: ./ats/infra/playwright/Dockerfile
    volumes:
      - ./ats/data-channels:/specs
      - ./ats/result:/result
    depends_on:
      - client
      - pion
    environment:
      PWOPTS: ${PWOPTS--x}
  client:
    image: halverneus/static-file-server:latest
    environment:
      PORT: 80
    expose:
      - "80"
    volumes:
      - ./examples/data-channels/jsfiddle:/web
  pion:
    build:
      context: .
      dockerfile: ./ats/infra/pion/Dockerfile
    expose:
      - "22"
    volumes:
      - .:/go/src/github.com/pion/webrtc
