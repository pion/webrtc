version: "3.9"
services:
  runner:
    build: 
      context: .
      dockerfile: ./ats/infra/runner/Dockerfile
    volumes:
      - ./ats/datachannels:/suite
      - ./ats/result:/result
    depends_on:
      - client
      - pion
    environment:
      PWARGS: ${PWARGS--x}
  client:
    image: halverneus/static-file-server:latest
    environment:
      PORT: 80
    expose:
      - "80"
    volumes:
      - ./ats/datachannels/client:/web
  pion:
    build:
      context: .
      dockerfile: ./ats/infra/pion/Dockerfile
    expose:
      - "22"
    volumes:
      - ./ats/datachannels:/source
